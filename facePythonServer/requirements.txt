# python == 3.10.20

numpy==1.24.3
opencv_python==4.10.0.84
torch==2.4.0
torchvision==0.19.0
websockets==16.0
pyyaml==6.0.2

# MediaPipe FaceLandmarker (with blendshapes) - 替换 open-closed-eye.onnx 的闭眼检测
# mediapipe 会附带拉 opencv-contrib-python，与上面的 opencv_python 有重叠，但两者共存不影响 import cv2 的使用
mediapipe==0.10.35
